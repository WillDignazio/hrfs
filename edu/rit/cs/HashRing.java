/**
 * Copyright © 2014
 * Hash Ring Object and Utilities
 *
 * @file HashRing.java
 * @author Will Dignazio <wdignazio@gmail.com>
 */

import java.util.List;
import java.util.HashMap;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class HashRing
{
	private List<HrfsRPC> rpcs;
	private 

	/**
	 * Slice of the hash ring for the node, contains metadata
	 * information on the hash region.
	 */
	private class Slice
	{
		private byte[] ba_key
		private String key;

		/** Construct hash ring slice */
		public Slice(String key)
		{
			if(key == NULL) {
		}
	}
}
